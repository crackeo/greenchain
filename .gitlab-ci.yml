image: node:22

stages: [verify]

verify:
  stage: verify
  cache:
    key:
      files: [package-lock.json]
    paths: [node_modules/]
  script:
    - npm ci
    - npm run typecheck
    - npm test
    - npm run build
  rules:
    - if: $CI_COMMIT_BRANCH
